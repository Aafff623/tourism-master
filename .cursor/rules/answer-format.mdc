---
description: 对用户回复的强制格式：Dual-Track（结论 + 过程与依据），按终端/IDE 环境切换顺序；信息密度优先
alwaysApply: true
---

# Answer Format

对用户的最终回复必须遵守本节。与 `humanizer-output-style` 叠加：人味 / 去 AI 腔管语气；本节管结构与信息密度。

## 动态触发

- **启用**：多步任务、架构决策、≥3 文件变更、需用户确认的歧义。
- **跳过**：单行修复、纯信息查询、简单是/否确认 → 单段回复即可，不套 Dual-Track。

## 结构：Dual-Track（结论 + 过程与依据）

非平凡回复必须且仅用两个模块；**顺序由环境决定**（见下），不可拆成更多顶层章节：

| 模块 | 篇幅 | 读者目标 |
|------|------|----------|
| **Conclusion（结论）** | 按复杂度缩放，≤15 行 | 扫一眼能决策：结论 / 现状 / 卡点 / 下一步 |
| **Process and Evidence（过程与依据）** | 不限 | 决策轨迹 / 执行清单 / 验证证据 |

**Conclusion 应回答三问**：做了什么或卡在哪 / 当前状态（数字 / 进度）/ 用户下一步动作。

**Conclusion 格式**（与 `AGENTS.mdc` Dual-Track 对齐）：优先精炼列表（≤15 行）——主题句 + 编号一行一项（动词开头、一事一行、不注水）；简单任务仍可用自然段落 + 1–3 要点。

**Process and Evidence 呈现**：以**表格**、**白话 Mermaid**（仅 IDE，见下方规范）或**思维导图（Markdown/ASCII）**为主，禁止大段连续散文。

## 环境适配（联动全局规范 §16）

- **终端 CLI**：先 `## Process and Evidence`，再 `## Conclusion`；**禁用 Mermaid**（渲染不出），改用「1 → 2 → 3」白话步骤或 ASCII 图。
- **IDE / 富文本面板**：先 `## Conclusion`，再 `## Process and Evidence`；允许 Mermaid（须遵守下方「白话图」规范）。

## 信息载体：密度优先

按优先序选用；能表不叙、能图不长文：

| 优先 | 载体 | 适用 |
|------|------|------|
| 1 | **表格** | 对比、确认项、验收、路径职责、阶段对照 |
| 2 | **Mermaid**（仅 IDE） | 流程、谁做什么、先后顺序、要不要分叉——用白话讲清楚 |
| 3 | 短列表 / 代码块 | 命令、路径树、检查清单 |
| 4 | 短段落 | 仅作衔接与判断说明，禁止大段纯叙述堆砌 |

## Mermaid 白话图规范（全局 · 跨项目）

画给用户看的图，默认按「讲故事」而不是「画程序内部结构」。与 `AGENTS.md` / `AGENTS.mdc` §16 同步，**所有项目一律遵守**：

1. **一眼能懂**：框里的字，不懂编程也能读明白。
2. **写人话**：谁 → 干什么 → 然后怎样。优先口语短句（如「你带来草稿」「我帮你排好」「网站自动生成目录」）。
3. **图上不写程序概念**：除非用户明确要求看代码级设计，否则不要在框里写函数名、方法名、类名，以及「调用 / 管道 / 处理器」这类开发黑话；细节放到图下的小表格或代码块。
4. **框短、表细**：每个框几个字就够；解释放图外。
5. **一张图一件事**：需要先解释一堆术语才能看懂 → 先改写成白话再画。
6. **终端里**：不要贴 Mermaid 代码；改成编号步骤说明同一条流程。

**好例子**：`你带来草稿和配图` → `我改成博客文章` → `你预览确认` → `网站上线给读者看`  
**坏例子**：`parseFrontmatter()` → `remarkPluginChain` → `render(entry)`（这是写给机器看的，不写进用户图）

## 反模式

- 先长文后结论，或只有过程没有结论
- 顶层拆成「背景 / 分析 / 建议 / 总结」等多段代替两模块
- CLI 下使用 Mermaid
- 用户图里堆函数名 / 类名 / 调用链，却不说人话
- 大段散文复述表格/图里已有的信息
- 客服腔、假热情、空洞强调「至关重要」
